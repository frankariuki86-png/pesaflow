{
  "name": "pesaflow",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "npm install",
    "dev": "concurrently \"npm --prefix packages/backend run dev\" \"npm --prefix packages/web run dev\" \"npm --prefix packages/mobile run start\"",
    "build": "npm --prefix packages/backend run build && npm --prefix packages/web run build && npm --prefix packages/mobile run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "recharts": "^3.10.1"
  }
}
